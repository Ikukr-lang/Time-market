services:
  backend:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    volumes: ["./backend:/app"]
    command: uvicorn app.main:app --host 0.0.0.0 --reload

  bot:
    build: .
    env_file: .env
    command: python bot/bot.py
